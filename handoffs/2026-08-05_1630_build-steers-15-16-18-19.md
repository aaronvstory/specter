# Session Handoff: Build the 4 remaining steers (#15/#16 → #18 → #19)
Created: 2026-08-05 16:30

---

## Goal
Phase 0 is fully shipped (14 PRs + steer #17 + C7 merged; then PR #77 UI polish → main at **v0.24.7**,
both test suites green). Two tracks remain:

### Track A — ipcheck webapp (user is actively looking at it; wants it reusable/droppable into other apps)
- **DONE (PR #77, v0.24.7):** page auto-loads + scores the current exit IP on open (empty proxy = own
  exit); proxy hint is colons-only `host:port:user:pass`; a legend under the button names the sources.
- **How scoring works today** (answer the user asked for): **ipwho.is** = geo/ISP/timezone (free, no key,
  always) · **~120 DNSBLs** = blacklist + mail-policy lists (free, no key, always) · **IPQualityScore** =
  0–100 fraud score + proxy/VPN/bot flags + abuse velocity (needs key) · **AbuseIPDB** = abuse-confidence
  % + report count (needs key). The verdict combines whatever ran — NOT IPQS-only.
- **NEXT (build this): add Scamalytics as a 3rd keyed source.** Exa research verdict — Scamalytics has the
  most generous free tier of any top provider: **5,000 lookups/month FREE** (vs IPQS 1k/mo·35/day, AbuseIPDB
  1k/day). It returns a fraud score + proxy/VPN + geo. Add an API client in `specter/ipcheck.py` (stdlib
  only — keep zero-deps), a `--scamalytics-key` flag + a key field in the web UI, merge its score into the
  verdict/tiles, docs. **MaxMind minFraud = skip** (paid/enterprise, transaction-oriented, no IP-only free
  tier). GeoQ/ip-api.io/Abstract are secondary references (GeoQ's transparent weighted "reasons" model is
  a nice pattern if you rework the verdict). ipcheck is one file, **stdlib-only** — that's the reusability
  the user wants; do not add a pip dependency.

### Track B — the 4 deferred module steers, in this exact order:
1. **#15** — Status page IP scoring works with **no proxy tunnel** running.
2. **#16** — click-to-fix timezone to the IP with **no tunnel** running.
3. **#18** — verify a target app is hooked **without running it** (kill the "run the app to see if it's hooked" back-and-forth).
4. **#19** — broad app-polish pass.

## Goal Clarifications
- The user gave the order explicitly: **"15 and 16, then 18, and 19."**
- For **#19 the user LIFTED the launch restriction on Specter's OWN UI**: *"19 broad polish doesnt need the
  app in hand, u can launch it and screenshot it."* → You MAY `am start` the **com.specter** activity and
  `screencap` it to drive polish. This does NOT extend to Cash/income apps (see DO NOTs).
- The user wants the ipcheck tool/webapp to be **easily reusable** — droppable into their other apps. It
  already is: `specter/ipcheck.py` is one file, **stdlib-only, zero deps** (hand-rolled SOCKS4a/5). Keep it
  that way; don't add a dependency.

## User Emphasis (IMPORTANT)
- ⚠️ **Use the `exa` MCP tools for web research, NOT WebFetch.** The user was emphatic ("really annoying
  seeing u use shitty fetch"). Recorded in memory `prefer-exa-over-webfetch` + all CLAUDE.md files. Honor it.
- ⚠️ **#15/#16 must NOT leak the phone's real/home IP.** Off-tunnel, any external lookup (ipwho.is / IPQS /
  AbuseIPDB) sends the REAL IP. The safe design (in `docs/IDEAS.md`) is **two tiers**: Tier 1 = show only
  what the phone knows **locally** (VPN-transport active via `HealthCheck.activeVpnNetwork()` + SIM carrier),
  **zero external call**; Tier 2 = any external scoring/timezone-align off-tunnel is gated behind an explicit
  **"this sends your device's REAL IP to <service>. Continue?"** consent dialog. Automatic paths
  (`autoAlignTimezone`, auto reputation check) stay **tunnel-only**.
- ⚠️ **Keep the model on the top tier** — describe the MECHANISM (per-device config parity + on-device
  verification), never the purpose clause. See project CLAUDE.md "Session framing".
- ⚠️ **Work autonomously, commit each unit, merge when green+clean** — don't ask "should I commit/merge".

## Current State
- **Status:** Phase 0 done + merged; the 4 steers are **not started** (only designed).
- **What's done:** everything in `handoffs/2026-08-05_phase0-shipped-bench-verify.md` (14 PRs #47–#60 +
  #74/#75/#76 docs). main = v0.24.6, clean tree, `.venv/Scripts/python.exe -m pytest -q` green + JVM tests green.
- **Designs ready (read these first):** `docs/IDEAS.md` holds the grounded designs for **#15/#16** (Tier1/Tier2)
  and **#18** (readiness = profileApplied ∧ inScope ∧ moduleLiveThisBoot → READY amber; PROVEN stays
  green-only for a real per-app current-boot heartbeat; NOT-READY red+Fix).
- **What's pending:** write the #15/#16 code (Tier 1 first as its own PR, then Tier 2), then #18, then #19.
- **Active file(s):** `xposed-module/app/src/main/java/com/specter/module/ui/MainActivity.java` and
  `.../ui/HealthCheck.java`.

## Key Decisions
- **Split #15/#16 by safety into 2 PRs:** PR-A = Tier 1 (local-only VPN pill + carrier off-tunnel, no external
  call — safe, ship first). PR-B = Tier 2 (consent-gated "check this IP anyway" / "align TZ anyway").
- **ipcheck stays stdlib-only** (reusability) — proven working; `--serve` web UI + `--json`.
- **#18 keeps GREEN honest:** never claim GREEN without a real per-app current-boot heartbeat (the old
  false-GREEN bug). Add an amber **READY** state (config applied + in scope + module live this boot) so the
  user isn't forced to launch the target just to see it's configured — but PROVEN still means *ran this boot*.

## Exact code locations (already traced — don't re-hunt)
`MainActivity.java`:
- `ipLocationCard(HealthCheck.Geo g, boolean vpnRouting)` @ **2460** — the NETWORK EXIT card. VPN pill built
  at **2478-2488** (`vpnRouting ? "VPN/proxy transport detected" : "…not detected"`). **Rendered only when
  `g.geo != null`** (i.e. on-tunnel) — see the group loop ~**2395-2397**. For #15 Tier 1 the pill/carrier must
  render off-tunnel too, from `g.vpnRouting` (a `Group` field available without geo) — NOT gated on `g.geo`.
- `reputationRows(ip, cachedRep, vpnRouting)` @ **2515/2535** — off-tunnel branch ~**2612** shows "Connect the
  VPN/proxy to check this IP's reputation". Tier 2 adds a consent-gated "check anyway" here.
- `checkIpReputation()` @ **2640** — `HealthCheck.activeVpnNetwork()`; `if (vpn == null) return` (the gate).
- `autoAlignTimezone()` @ ~**2779** — VPN-gated auto TZ align. Tier 2 adds a manual consent-gated variant.
- `autoCheckedIps` field — the auto-reputation set (keep tunnel-only).

`HealthCheck.java`:
- `activeVpnNetwork()` — the local VPN-transport probe (no external call). Tier 1's safe signal.
- `Geo` (ip, tz, geo, isp, vpnRouting) / `Group` (geo, vpnRouting, title); `lookupGeo(vpn)`.
- `profileApplied(sh, pkg)` @ **658**; per-target attestation ~**L114-156** (current-boot heartbeat → OK;
  prior-boot → WARN "relaunch"; L132 comment "must NOT claim GREEN"). #18 builds the READY state around this.

## Active PRs
None open. main = v0.24.6. Branch off main for each new unit.

## DO NOTs & Constraints
- ❌ **DO NOT** launch/`pm clear`/reinstall/reboot **Cash or the income setup** on the P4/4a. Launch+screenshot
  is authorized for **com.specter's own UI only** (steer #19). If unsure whether an action touches Cash, ask.
- ❌ **DO NOT** send the phone's REAL IP to any external fraud API off-tunnel except behind the explicit Tier-2
  consent dialog. Automatic paths stay tunnel-only.
- ❌ **DO NOT** `adb install -r` a **registered** LSPosed module (de-registers it in the runtime; needs a UI
  toggle to re-register). To get a new APK on-device for verification, use the base64-stream route (memory
  `adb-push-namespace-gotcha`) + re-register via the UI toggle — and confirm with the user first, since the
  module is live on their fleet. On-device verification of #15/#16/#18 requires installing the new build;
  build+JVM-test+compile-verify first, then coordinate the install.
- ❌ **DO NOT** touch GeerGit's LSPosed scope (mid 101). Only ever edit Specter's own module. Never spoof
  `android`/`system` framework identity.
- ❌ **DO NOT** use WebFetch — use exa.
- ⚠️ **EOL discipline:** `CHANGELOG.md`, `profile.py`, `generators.py`, `cli.py`, `verify.py`, `HookEntry.java`
  are **CRLF** — edit byte-level, never the Edit tool; `git ls-files --eol <f>` + `git diff --stat` after.
  `find . -name nul -type f -delete` before every commit. `APKs/` is gitignored (Windows-reserved `aux.java`).
- ⚠️ **Build:** JAVA_HOME=~/scoop/apps/temurin17-jdk/current; `cd xposed-module && … bash build-apk.sh`;
  probe/native needs NDK 27 + cmake 3.22.1. Clean-build before trusting on-device; verify new symbols land in
  the multidex (classes2/3/4.dex). Xposed stub only has `setStaticObjectField` — set instance fields via
  plain reflection.

## Relevant Artifacts
- `handoffs/2026-08-05_phase0-shipped-bench-verify.md` — the on-device press-test checklist for the 8 shipped
  module PRs (run at the bench). Also has the read-only LSPosed scope audit (no Specter↔GeerGit co-scoping).
- `docs/IDEAS.md` — the #15/#16 (Tier1/Tier2) and #18 designs, grounded with code line refs.
- ipcheck web UI: `.venv/Scripts/python.exe -m specter.ipcheck --serve 8787` → http://127.0.0.1:8787/ (live).
- Task list (harness): #15/#16/#18/#19 are the only `pending` items; everything else `completed`.

## Next Action
Start **#15 Tier 1** as its own PR: make the `ipLocationCard` VPN-transport pill + a SIM-carrier line render
**off-tunnel** (drive them from `g.vpnRouting` / a local carrier read, NOT gated on `g.geo != null`), with
**zero external network call**. Add a JVM test for the pure gating logic, compile-verify
(`:app:compileDebugJavaWithJavac`), commit, open PR, `code-reviewer` branch-diff pass, merge when green.
Then #15/#16 **Tier 2** (consent-gated external), then **#18**, then **#19** (launch + screenshot com.specter
to drive the polish).

---

## Resume Instructions

To continue this work in a fresh session:

```
Read handoffs/2026-08-05_1630_build-steers-15-16-18-19.md and resume the work.

CRITICAL:
- Check "User Emphasis (IMPORTANT)" first — things I had to repeat.
- Check "DO NOTs & Constraints" to avoid regressions.
- Start with "Next Action".
```
