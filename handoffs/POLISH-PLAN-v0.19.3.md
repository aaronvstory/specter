# Status/Settings polish — codex implementation plan (v0.19.3)

Generated by /codex 2026-07-30 on branch feat/status-settings-polish. Concrete file:line + exact copy.
HARD RULE (user): NO paragraphs / multi-sentence blocks ANYWHERE — one short line or bullets.

## 1. Settings structure

- File: `MainActivity.java:619-620`
- Change: suppress global setup banner on Settings tab.
- Exact condition:

```java
if (tab != 2 && !prefs.getBoolean("setup_done", false) && !setupScreen) {
    content.addView(setupBanner());
}
```

- Result: banner remains on Identity/Vault until setup completes.
- Result: Settings contains one setup entry only.
- Type: layout behavior.

- File: `MainActivity.java:1649-1663`
- Replace combined `Status` card.
- Exact structure:

```java
content.addView(sectionLabel("Setup"));
LinearLayout setupCard = card();
LinearLayout sTrail = new LinearLayout(this);
sTrail.addView(chevronTrailing(false));
setupCard.addView(row(
        "Set up everything",
        "Install layers, scope apps, then reboot",
        sTrail,
        v -> {
            setupScreen = true;
            setupResults = null;
            render();
        }));
content.addView(setupCard);

content.addView(sectionLabel("Status"));
LinearLayout statusCard = card();
LinearLayout hTrail = new LinearLayout(this);
hTrail.addView(chevronTrailing(false));
statusCard.addView(row(
        "Check protection status",
        "Verify setup, hooks, location, and network",
        hTrail,
        v -> {
            healthScreen = true;
            render();
        }));
content.addView(statusCard);
```

- Type: layout + copy.

- File: `MainActivity.java:1691-1692`
- Change: exclude diagnostics from `protectionsCard()`.
- Exact call:

```java
content.addView(sectionLabel("Protections"));
content.addView(protectionsCard(false));
```

- File: `MainActivity.java:1693`
- Add diagnostics section before `Advanced (root)`.
- Exact structure:

```java
content.addView(sectionLabel("Diagnostics"));
content.addView(diagnosticsCard());
```

- File: `MainActivity.java:2300-2308`
- Replace `protectionsCard()`.
- Exact structure:

```java
private View protectionsCard(boolean diagnosticsOnly) {
    LinearLayout c = card();
    boolean first = true;
    for (Protections.P prot : Protections.ALL) {
        boolean diagnostic = "trace".equals(prot.gateKey);
        if (diagnostic != diagnosticsOnly) continue;
        if (!first) c.addView(hairlineInset());
        c.addView(protectionRowInner(prot));
        first = false;
    }
    return c;
}

private View diagnosticsCard() {
    return protectionsCard(true);
}
```

- Result: `Read logging` and `View live trace` remain together.
- Result: diagnostics no longer appear under Protections.
- Type: layout only.

## 2. Settings copy

- File: `MainActivity.java:1685-1688`
- Keep title:

```text
Device identity — always applied
```

- Replace description:

```text
Model, build, hardware, and sensors match the applied phone
```

- Exact code:

```java
TextView desc = value("Model, build, hardware, and sensors match the applied phone");
```

- Type: copy only.

- File: `MainActivity.java:727`
- Replace banner description:

```text
Install layers, scope apps, then reboot
```

- Type: copy only.

## 3. Protection-row copy

- File: `Protections.java:48-65`
- Replace `ALL`.
- Exact copy:

```java
public static final P[] ALL = {
    new P("hide_root",     "Hide root",              "Root tools read as absent"),
    new P("hide_mock",     "Hide mock location",     "Mock-location flags read as clean"),
    new P("hide_dev",      "Hide developer mode",    "ADB and Developer Options read as off"),
    new P("hide_apps",     "Hide app list",          "Specter and root tools stay out of app lists"),
    new P("hide_vpn",      "Hide VPN interfaces",    "VPN interfaces stay hidden from scoped apps"),
    new P("fix_webrtc",    "Fix WebRTC leak",        "WebRTC exposes only the routed address"),
    new P("spoof_ua",      "Match browser signature","User-Agent matches the applied phone"),
    new P("spoof_apktime", "Randomize install time", "Install time changes with the identity"),
    new P("spoof_sysfs",   "Match hardware profile", "Hardware and display signals match the profile"),
    new P("spoof_codecs",  "Match media codecs",     "Codec names match the applied phone", true),
    new P("trace",          "Read logging",           "Log reads without changing returned values", false),
};
```

- Type: copy.
- `hide_mock`: behavior addition.
- `hide_vpn` label: removes unsupported plain-proxy claim.

- File: `MainActivity.java:2349-2353`
- Replace status messages.
- Exact copy:

```java
status.setText(on
        ? "Read logging on — APPLY to arm"
        : "Read logging off");
```

```java
status.setText(prot.label + (on ? " on — APPLY to push" : " off — APPLY to push"));
```

- Type: copy only.

## 4. Widevine default

- Recommendation: default ON for new installs.
- Recommendation: keep visible opt-out.
- Recommendation: never silently modify `/vendor` during screen render.
- Recommendation: apply default through explicit setup flow.
- Recommendation: preserve existing-user state.
- Tradeoff copy:

```text
Device-wide software DRM · disables HD Netflix and Prime
```

- File: `MainActivity.java:2201`
- Recommended label:

```text
Force Widevine L3
```

- File: `MainActivity.java:2204,2213`
- Do not blindly replace `false` with `true`.
- Reason: switch could show ON without installed Magisk module.
- Type: correctness constraint.

- File: `MainActivity.java`, preference initialization near `onCreate`
- Add one-time default seeding.
- Exact behavior:

```java
if (!prefs.contains("widevine_l3")) {
    prefs.edit().putBoolean("widevine_l3", true).apply();
}
```

- Restrict seeding to fresh installs.
- Preserve legacy users through a migration marker.
- Suggested marker:

```text
widevine_default_initialized
```

- Migration rule:
  - Fresh install: `widevine_l3=true`.
  - Existing install: detect actual module state.
  - Existing module present: store `true`.
  - Existing module absent: store `false`.

- File: `SetupFlow.java:121-128`
- Change: accept `boolean enableWidevine`.
- Behavior:
  - `true`: run `WidevineL3.install(shell)`.
  - `false`: skip Widevine step.
- Exact skipped detail:

```text
Skipped by user setting
```

- File: `MainActivity.java:1846-1855`
- Pass:

```java
prefs.getBoolean("widevine_l3", true)
```

- File: `MainActivity.java:2207`
- Exact replacement:

```java
TextView d = value("Device-wide software DRM · disables HD Netflix and Prime");
```

- Type: behavior + copy.
- Safety: explicit setup remains mutation boundary.
- Safety: toggle remains reversible.
- Safety: OFF removes module through existing uninstall path.

## 5. Network card redesign

- File: `MainActivity.java:2002-2066`
- Replace `ipLocationCard()`.
- Remove:
  - `🌐`
  - `📍`
  - `🕑`
  - emoji-dependent spacing
  - amber “No VPN”
  - misleading proxy inference

- Pill copy:

```text
VPN transport detected
```

```text
VPN transport not detected
```

- Pill colors:
  - detected: `Theme.SAGE`
  - not detected: `Theme.BLUE`
- Reason: transport absence is informational.
- Reason: transport absence does not prove direct routing.

- Header copy:

```text
NETWORK EXIT
```

- Public-IP support line:

```text
Public IP shows the network exit
```

- Detection-limit line:

```text
Upstream VPNs and plain proxies are not detectable here
```

- Exact layout order:
  - Header label + transport pill
  - Large public IP
  - ISP
  - Hairline
  - `LOCATION` + city/region/country
  - `TIME ZONE` + IP timezone
  - Hairline
  - Public-IP support line
  - Detection-limit line

- Add helper near `ipLocationCard()`:

```java
private View networkMetaRow(String labelText, String valueText) {
    LinearLayout row = new LinearLayout(this);
    row.setOrientation(LinearLayout.HORIZONTAL);
    row.setGravity(Gravity.TOP);
    row.setPadding(0, dp(Theme.S2), 0, 0);

    TextView label = new TextView(this);
    label.setText(labelText);
    label.setTextColor(Theme.DIM);
    label.setTextSize(Theme.T_CAPTION);
    label.setTypeface(android.graphics.Typeface.create(
            "sans-serif-medium",
            android.graphics.Typeface.BOLD));
    row.addView(label, new LinearLayout.LayoutParams(dp(88), ViewGroup.LayoutParams.WRAP_CONTENT));

    TextView value = new TextView(this);
    value.setText(valueText);
    value.setTextColor(Theme.INK);
    value.setTextSize(Theme.T_LABEL);
    row.addView(value, new LinearLayout.LayoutParams(
            0,
            ViewGroup.LayoutParams.WRAP_CONTENT,
            1f));

    return row;
}
```

- Exact calls:

```java
card.addView(networkMetaRow("LOCATION", g.location()));
```

```java
card.addView(networkMetaRow("TIME ZONE", g.tz));
```

- Type: layout + copy.

- File: `HealthCheck.java:242-259`
- Drop `Routing` warning row.
- Reason: card already states detection boundary.
- Reason: missing VPN transport cannot prove direct traffic.
- Keep `routedThroughVpn` only as timezone auto-match safety gate.
- Type: layout behavior.

- File: `HealthCheck.java:230-239`
- Rename row:

```text
VPN interface masking
```

- Exact details:

```text
Java and native VPN interfaces are hidden
```

```text
Native masking unavailable · install the native layer
```

```text
Off · scoped apps can read VPN interfaces
```

- Type: copy only.

- File: `HealthCheck.java:279-288`
- Exact timezone copy:

```java
"Device: " + mismatch + " · IP: " + g.tz
```

```java
"Device timezone matches IP · " + g.tz
```

```text
Connect an on-device VPN before auto-matching
```

- Type: copy only.

## 6. Mock-location gate

- File: `HookEntry.java:90`
- Replace gate:

```java
if (!gateOff(p, "hide_mock")) hookMockLocation(lpparam);
```

- Result: mock hiding no longer depends on `hide_root`.
- Existing profiles: missing `hide_mock` remains ON.
- Type: behavior.

- File: `Protections.java:49`
- Add dedicated protection:

```java
new P("hide_mock", "Hide mock location", "Mock-location flags read as clean"),
```

- Default: ON.
- Type: behavior + UI.

- File: `MainActivity.java:2384-2403`
- Update location view if retained.
- Replace `hide_root` lookup:

```java
boolean on = Protections.isOn(prefs, Protections.byKey("hide_mock"));
```

- Exact title:

```text
Hide mock location
```

- Exact description:

```text
Mock-location flags read as clean in scoped apps
```

- Remove:

```text
Follows the Hide-root toggle
```

- Type: behavior + copy.

- HideMyMock clarification:
  - Specter performs its own Xposed hooks.
  - Specter does not configure `io.github.auag0.hidemocklocation`.
  - HideMyMock installation must not affect Specter health status.
  - Do not add HideMyMock controls or package-management UI.

## 7. Mock-location health state

- File: `HealthCheck.java:169-170`
- Change call:

```java
groups.add(new Group(
        "Location",
        java.util.Collections.singletonList(mockLocationCheck(ctx, prefs, sh))));
```

- File: `HealthCheck.java:178-214`
- Replace package/app-op warning logic.
- Do not warn because Lockito is installed or selected.
- Read only:
  - `hide_mock` preference
  - legacy `Settings.Secure.mock_location`
- Scoped execution proof remains in existing target-app heartbeat checks.

- Recommended logic:

```java
private static Check mockLocationCheck(
        Context ctx,
        SharedPreferences prefs,
        RootWriter.Shell sh) {
    Protections.P protection = Protections.byKey("hide_mock");
    boolean enabled = protection != null && Protections.isOn(prefs, protection);

    if (!enabled) {
        return Check.warn(
                "Mock location",
                "Off · scoped apps can read mock-location flags",
                Fix.NONE,
                null);
    }

    try {
        String legacy = sh.runCapture(
                "settings get secure mock_location 2>/dev/null").trim();

        if ("1".equals(legacy)) {
            return Check.ok(
                    "Mock location",
                    "Hidden in scoped apps · device flag is on");
        }

        return Check.ok(
                "Mock location",
                "Hidden in scoped apps");
    } catch (Throwable t) {
        return Check.ok(
                "Mock location",
                "Hidden in scoped apps · device flag unavailable");
    }
}
```

- Green state:
  - `hide_mock` ON.
  - Target app scoped.
  - Target-app heartbeat green elsewhere.
- Warn state:

```text
Off · scoped apps can read mock-location flags
```

- Optional device-wide warning:
  - Do not downgrade Specter hook status.
  - Show only as suffix:

```text
Hidden in scoped apps · device flag is on
```

- Type: behavior + copy.
- Result: normal Lockito use can reach green.
- Result: installed mocker no longer creates false warning.

## 8. Remaining HealthCheck terseness

- File: `HealthCheck.java:80-87`
- Exact replacements:

```text
Scoped, but not loaded · reboot or toggle scope
```

```text
Enable System Framework scope, then reboot
```

- File: `HealthCheck.java:96`
- Exact replacement:

```text
Check unavailable · verify root and bundled assets
```

- File: `HealthCheck.java:99`
- Exact replacement:

```text
Not installed · native reads remain unmasked
```

- File: `HealthCheck.java:102-103`
- Exact replacement:

```java
"Installed " + nn(z.installedVersion) + " · bundled " + nn(z.bundledVersion)
```

- File: `HealthCheck.java:105`
- Exact replacement:

```java
"Installed and current · " + nn(z.installedVersion)
```

- File: `HealthCheck.java:126`
- Exact replacement:

```text
Not scoped · add in LSPosed, then reboot
```

- File: `HealthCheck.java:129`
- Exact replacement:

```text
Scoped · no identity applied
```

- File: `HealthCheck.java:150-151`
- Exact replacement:

```java
"Hooks loaded this boot · " + hb.fields + " profile fields · v" + hb.version
```

- File: `HealthCheck.java:153-155`
- Exact replacement:

```java
"Old module loaded · " + hb.version + " → " + HookEntry.MODULE_VERSION
```

- File: `HealthCheck.java:157-158`
- Exact replacement:

```text
Last verified before this boot · relaunch app
```

- File: `HealthCheck.java:160-162`
- Exact replacement:

```text
Hooks unverified · open app, then re-check
```

- File: `HealthCheck.java:266`
- Exact replacement:

```text
IP lookup unavailable · check network
```

- Type: copy only.

## 9. MainActivity health summary copy

- File: `MainActivity.java:1937-1940`
- Replace multi-clause details.
- Exact copy:

```java
String heroSub = bad > 0
        ? bad + " failed · " + warn + " unverified"
        : warn > 0
                ? warn + " unverified"
                : total + " checks passed";
```

- Type: copy only.

## 10. Files reviewed with no planned change

- `Theme.java`
  - Existing charcoal, gold, sage, amber, blue tokens sufficient.
- `Nav.java`
  - Existing back-row structure sufficient.
- `HookEntry.java:189-220`
  - Hook implementation already covers:
    - `Location.isFromMockProvider()`
    - `Location.isMock()`
    - `Settings.Secure.mock_location`
    - `Settings.System.mock_location`
  - Only gate key changes.

## 11. Verification

- Add JVM tests:
  - `hide_mock` defaults ON.
  - `hide_mock=0` serializes into profile.
  - missing `hide_mock` keeps legacy profiles protected.
  - `hookMockLocation` uses `hide_mock`.
  - diagnostics excluded from Protections card logic.
  - Widevine opt-out skips setup installation.

- Add copy guard test:
  - Scan `Protections.ALL[*].desc`.
  - Reject newline.
  - Reject more than one sentence terminator.
  - Set short length ceiling, e.g. `80`.

- Manual UI checks:
  - Settings shows `Setup`, `Status`, `Anti-fingerprinting`, `Protections`, `Diagnostics`, `Advanced`.
  - Settings shows no `Finish setup` banner.
  - Network labels align without emoji.
  - Both pill states fit narrow screens.
  - Lockito installed + `hide_mock` ON yields green.
  - `hide_mock` OFF yields warning.
  - HideMyMock installed or absent changes nothing.
  - Widevine OFF survives setup rerun.
  - Fresh-install setup applies Widevine by default.

- Commands:

```text
.venv/Scripts/python.exe -m pytest -q
```

```text
cd xposed-module && bash run-jvm-tests.sh
```

- Type: verification only.
tokens used
94,425
# Ordered PR plan

- Current line numbers; re-check after edits.
- Scope: native Views only.
- Theme: reuse `Theme.*`; no new colors.
- App copy rule: one short line per `TextView`.
- App copy rule: no description with multiple sentences.
- App copy rule: use separate bullet rows for multi-step guidance.

## 1. Settings structure

- File: `MainActivity.java:619-620`
- Change: suppress global setup banner on Settings tab.
- Exact condition:

```java
if (tab != 2 && !prefs.getBoolean("setup_done", false) && !setupScreen) {
    content.addView(setupBanner());
}
```

- Result: banner remains on Identity/Vault until setup completes.
- Result: Settings contains one setup entry only.
- Type: layout behavior.

- File: `MainActivity.java:1649-1663`
- Replace combined `Status` card.
- Exact structure:

```java
content.addView(sectionLabel("Setup"));
LinearLayout setupCard = card();
LinearLayout sTrail = new LinearLayout(this);
sTrail.addView(chevronTrailing(false));
setupCard.addView(row(
        "Set up everything",
        "Install layers, scope apps, then reboot",
        sTrail,
        v -> {
            setupScreen = true;
            setupResults = null;
            render();
        }));
content.addView(setupCard);

content.addView(sectionLabel("Status"));
LinearLayout statusCard = card();
LinearLayout hTrail = new LinearLayout(this);
hTrail.addView(chevronTrailing(false));
statusCard.addView(row(
        "Check protection status",
        "Verify setup, hooks, location, and network",
        hTrail,
        v -> {
            healthScreen = true;
            render();
        }));
content.addView(statusCard);
```

- Type: layout + copy.

- File: `MainActivity.java:1691-1692`
- Change: exclude diagnostics from `protectionsCard()`.
- Exact call:

```java
content.addView(sectionLabel("Protections"));
content.addView(protectionsCard(false));
```

- File: `MainActivity.java:1693`
- Add diagnostics section before `Advanced (root)`.
- Exact structure:

```java
content.addView(sectionLabel("Diagnostics"));
content.addView(diagnosticsCard());
```

- File: `MainActivity.java:2300-2308`
- Replace `protectionsCard()`.
- Exact structure:

```java
private View protectionsCard(boolean diagnosticsOnly) {
    LinearLayout c = card();
    boolean first = true;
    for (Protections.P prot : Protections.ALL) {
        boolean diagnostic = "trace".equals(prot.gateKey);
        if (diagnostic != diagnosticsOnly) continue;
        if (!first) c.addView(hairlineInset());
        c.addView(protectionRowInner(prot));
        first = false;
    }
    return c;
}

private View diagnosticsCard() {
    return protectionsCard(true);
}
```

- Result: `Read logging` and `View live trace` remain together.
- Result: diagnostics no longer appear under Protections.
- Type: layout only.

## 2. Settings copy

- File: `MainActivity.java:1685-1688`
- Keep title:

```text
Device identity — always applied
```

- Replace description:

```text
Model, build, hardware, and sensors match the applied phone
```

- Exact code:

```java
TextView desc = value("Model, build, hardware, and sensors match the applied phone");
```

- Type: copy only.

- File: `MainActivity.java:727`
- Replace banner description:

```text
Install layers, scope apps, then reboot
```

- Type: copy only.

## 3. Protection-row copy

- File: `Protections.java:48-65`
- Replace `ALL`.
- Exact copy:

```java
public static final P[] ALL = {
    new P("hide_root",     "Hide root",              "Root tools read as absent"),
    new P("hide_mock",     "Hide mock location",     "Mock-location flags read as clean"),
    new P("hide_dev",      "Hide developer mode",    "ADB and Developer Options read as off"),
    new P("hide_apps",     "Hide app list",          "Specter and root tools stay out of app lists"),
    new P("hide_vpn",      "Hide VPN interfaces",    "VPN interfaces stay hidden from scoped apps"),
    new P("fix_webrtc",    "Fix WebRTC leak",        "WebRTC exposes only the routed address"),
    new P("spoof_ua",      "Match browser signature","User-Agent matches the applied phone"),
    new P("spoof_apktime", "Randomize install time", "Install time changes with the identity"),
    new P("spoof_sysfs",   "Match hardware profile", "Hardware and display signals match the profile"),
    new P("spoof_codecs",  "Match media codecs",     "Codec names match the applied phone", true),
    new P("trace",          "Read logging",           "Log reads without changing returned values", false),
};
```

- Type: copy.
- `hide_mock`: behavior addition.
- `hide_vpn` label: removes unsupported plain-proxy claim.

- File: `MainActivity.java:2349-2353`
- Replace status messages.
- Exact copy:

```java
status.setText(on
        ? "Read logging on — APPLY to arm"
        : "Read logging off");
```

```java
status.setText(prot.label + (on ? " on — APPLY to push" : " off — APPLY to push"));
```

- Type: copy only.

## 4. Widevine default

- Recommendation: default ON for new installs.
- Recommendation: keep visible opt-out.
- Recommendation: never silently modify `/vendor` during screen render.
- Recommendation: apply default through explicit setup flow.
- Recommendation: preserve existing-user state.
- Tradeoff copy:

```text
Device-wide software DRM · disables HD Netflix and Prime
```

- File: `MainActivity.java:2201`
- Recommended label:

```text
Force Widevine L3
```

- File: `MainActivity.java:2204,2213`
- Do not blindly replace `false` with `true`.
- Reason: switch could show ON without installed Magisk module.
- Type: correctness constraint.

- File: `MainActivity.java`, preference initialization near `onCreate`
- Add one-time default seeding.
- Exact behavior:

```java
if (!prefs.contains("widevine_l3")) {
    prefs.edit().putBoolean("widevine_l3", true).apply();
}
```

- Restrict seeding to fresh installs.
- Preserve legacy users through a migration marker.
- Suggested marker:

```text
widevine_default_initialized
```

- Migration rule:
  - Fresh install: `widevine_l3=true`.
  - Existing install: detect actual module state.
  - Existing module present: store `true`.
  - Existing module absent: store `false`.

- File: `SetupFlow.java:121-128`
- Change: accept `boolean enableWidevine`.
- Behavior:
  - `true`: run `WidevineL3.install(shell)`.
  - `false`: skip Widevine step.
- Exact skipped detail:

```text
Skipped by user setting
```

- File: `MainActivity.java:1846-1855`
- Pass:

```java
prefs.getBoolean("widevine_l3", true)
```

- File: `MainActivity.java:2207`
- Exact replacement:

```java
TextView d = value("Device-wide software DRM · disables HD Netflix and Prime");
```

- Type: behavior + copy.
- Safety: explicit setup remains mutation boundary.
- Safety: toggle remains reversible.
- Safety: OFF removes module through existing uninstall path.

## 5. Network card redesign

- File: `MainActivity.java:2002-2066`
- Replace `ipLocationCard()`.
- Remove:
  - `🌐`
  - `📍`
  - `🕑`
  - emoji-dependent spacing
  - amber “No VPN”
  - misleading proxy inference

- Pill copy:

```text
VPN transport detected
```

```text
VPN transport not detected
```

- Pill colors:
  - detected: `Theme.SAGE`
  - not detected: `Theme.BLUE`
- Reason: transport absence is informational.
- Reason: transport absence does not prove direct routing.

- Header copy:

```text
NETWORK EXIT
```

- Public-IP support line:

```text
Public IP shows the network exit
```

- Detection-limit line:

```text
Upstream VPNs and plain proxies are not detectable here
```

- Exact layout order:
  - Header label + transport pill
  - Large public IP
  - ISP
  - Hairline
  - `LOCATION` + city/region/country
  - `TIME ZONE` + IP timezone
  - Hairline
  - Public-IP support line
  - Detection-limit line

- Add helper near `ipLocationCard()`:

```java
private View networkMetaRow(String labelText, String valueText) {
    LinearLayout row = new LinearLayout(this);
    row.setOrientation(LinearLayout.HORIZONTAL);
    row.setGravity(Gravity.TOP);
    row.setPadding(0, dp(Theme.S2), 0, 0);

    TextView label = new TextView(this);
    label.setText(labelText);
    label.setTextColor(Theme.DIM);
    label.setTextSize(Theme.T_CAPTION);
    label.setTypeface(android.graphics.Typeface.create(
            "sans-serif-medium",
            android.graphics.Typeface.BOLD));
    row.addView(label, new LinearLayout.LayoutParams(dp(88), ViewGroup.LayoutParams.WRAP_CONTENT));

    TextView value = new TextView(this);
    value.setText(valueText);
    value.setTextColor(Theme.INK);
    value.setTextSize(Theme.T_LABEL);
    row.addView(value, new LinearLayout.LayoutParams(
            0,
            ViewGroup.LayoutParams.WRAP_CONTENT,
            1f));

    return row;
}
```

- Exact calls:

```java
card.addView(networkMetaRow("LOCATION", g.location()));
```

```java
card.addView(networkMetaRow("TIME ZONE", g.tz));
```

- Type: layout + copy.

- File: `HealthCheck.java:242-259`
- Drop `Routing` warning row.
- Reason: card already states detection boundary.
- Reason: missing VPN transport cannot prove direct traffic.
- Keep `routedThroughVpn` only as timezone auto-match safety gate.
- Type: layout behavior.

- File: `HealthCheck.java:230-239`
- Rename row:

```text
VPN interface masking
```

- Exact details:

```text
Java and native VPN interfaces are hidden
```

```text
Native masking unavailable · install the native layer
```

```text
Off · scoped apps can read VPN interfaces
```

- Type: copy only.

- File: `HealthCheck.java:279-288`
- Exact timezone copy:

```java
"Device: " + mismatch + " · IP: " + g.tz
```

```java
"Device timezone matches IP · " + g.tz
```

```text
Connect an on-device VPN before auto-matching
```

- Type: copy only.

## 6. Mock-location gate

- File: `HookEntry.java:90`
- Replace gate:

```java
if (!gateOff(p, "hide_mock")) hookMockLocation(lpparam);
```

- Result: mock hiding no longer depends on `hide_root`.
- Existing profiles: missing `hide_mock` remains ON.
- Type: behavior.

- File: `Protections.java:49`
- Add dedicated protection:

```java
new P("hide_mock", "Hide mock location", "Mock-location flags read as clean"),
```

- Default: ON.
- Type: behavior + UI.

- File: `MainActivity.java:2384-2403`
- Update location view if retained.
- Replace `hide_root` lookup:

```java
boolean on = Protections.isOn(prefs, Protections.byKey("hide_mock"));
```

- Exact title:

```text
Hide mock location
```

- Exact description:

```text
Mock-location flags read as clean in scoped apps
```

- Remove:

```text
Follows the Hide-root toggle
```

- Type: behavior + copy.

- HideMyMock clarification:
  - Specter performs its own Xposed hooks.
  - Specter does not configure `io.github.auag0.hidemocklocation`.
  - HideMyMock installation must not affect Specter health status.
  - Do not add HideMyMock controls or package-management UI.

## 7. Mock-location health state

- File: `HealthCheck.java:169-170`
- Change call:

```java
groups.add(new Group(
        "Location",
        java.util.Collections.singletonList(mockLocationCheck(ctx, prefs, sh))));
```

- File: `HealthCheck.java:178-214`
- Replace package/app-op warning logic.
- Do not warn because Lockito is installed or selected.
- Read only:
  - `hide_mock` preference
  - legacy `Settings.Secure.mock_location`
- Scoped execution proof remains in existing target-app heartbeat checks.

- Recommended logic:

```java
private static Check mockLocationCheck(
        Context ctx,
        SharedPreferences prefs,
        RootWriter.Shell sh) {
    Protections.P protection = Protections.byKey("hide_mock");
    boolean enabled = protection != null && Protections.isOn(prefs, protection);

    if (!enabled) {
        return Check.warn(
                "Mock location",
                "Off · scoped apps can read mock-location flags",
                Fix.NONE,
                null);
    }

    try {
        String legacy = sh.runCapture(
                "settings get secure mock_location 2>/dev/null").trim();

        if ("1".equals(legacy)) {
            return Check.ok(
                    "Mock location",
                    "Hidden in scoped apps · device flag is on");
        }

        return Check.ok(
                "Mock location",
                "Hidden in scoped apps");
    } catch (Throwable t) {
        return Check.ok(
                "Mock location",
                "Hidden in scoped apps · device flag unavailable");
    }
}
```

- Green state:
  - `hide_mock` ON.
  - Target app scoped.
  - Target-app heartbeat green elsewhere.
- Warn state:

```text
Off · scoped apps can read mock-location flags
```

- Optional device-wide warning:
  - Do not downgrade Specter hook status.
  - Show only as suffix:

```text
Hidden in scoped apps · device flag is on
```

- Type: behavior + copy.
- Result: normal Lockito use can reach green.
- Result: installed mocker no longer creates false warning.

## 8. Remaining HealthCheck terseness

- File: `HealthCheck.java:80-87`
- Exact replacements:

```text
Scoped, but not loaded · reboot or toggle scope
```

```text
Enable System Framework scope, then reboot
```

- File: `HealthCheck.java:96`
- Exact replacement:

```text
Check unavailable · verify root and bundled assets
```

- File: `HealthCheck.java:99`
- Exact replacement:

```text
Not installed · native reads remain unmasked
```

- File: `HealthCheck.java:102-103`
- Exact replacement:

```java
"Installed " + nn(z.installedVersion) + " · bundled " + nn(z.bundledVersion)
```

- File: `HealthCheck.java:105`
- Exact replacement:

```java
"Installed and current · " + nn(z.installedVersion)
```

- File: `HealthCheck.java:126`
- Exact replacement:

```text
Not scoped · add in LSPosed, then reboot
```

- File: `HealthCheck.java:129`
- Exact replacement:

```text
Scoped · no identity applied
```

- File: `HealthCheck.java:150-151`
- Exact replacement:

```java
"Hooks loaded this boot · " + hb.fields + " profile fields · v" + hb.version
```

- File: `HealthCheck.java:153-155`
- Exact replacement:

```java
"Old module loaded · " + hb.version + " → " + HookEntry.MODULE_VERSION
```

- File: `HealthCheck.java:157-158`
- Exact replacement:

```text
Last verified before this boot · relaunch app
```

- File: `HealthCheck.java:160-162`
- Exact replacement:

```text
Hooks unverified · open app, then re-check
```

- File: `HealthCheck.java:266`
- Exact replacement:

```text
IP lookup unavailable · check network
```

- Type: copy only.

## 9. MainActivity health summary copy

- File: `MainActivity.java:1937-1940`
- Replace multi-clause details.
- Exact copy:

```java
String heroSub = bad > 0
        ? bad + " failed · " + warn + " unverified"
        : warn > 0
                ? warn + " unverified"
                : total + " checks passed";
```

- Type: copy only.

## 10. Files reviewed with no planned change

- `Theme.java`
  - Existing charcoal, gold, sage, amber, blue tokens sufficient.
- `Nav.java`
  - Existing back-row structure sufficient.
- `HookEntry.java:189-220`
  - Hook implementation already covers:
    - `Location.isFromMockProvider()`
    - `Location.isMock()`
    - `Settings.Secure.mock_location`
    - `Settings.System.mock_location`
  - Only gate key changes.

## 11. Verification

- Add JVM tests:
  - `hide_mock` defaults ON.
  - `hide_mock=0` serializes into profile.
  - missing `hide_mock` keeps legacy profiles protected.
  - `hookMockLocation` uses `hide_mock`.
  - diagnostics excluded from Protections card logic.
  - Widevine opt-out skips setup installation.

- Add copy guard test:
  - Scan `Protections.ALL[*].desc`.
  - Reject newline.
  - Reject more than one sentence terminator.
  - Set short length ceiling, e.g. `80`.

- Manual UI checks:
  - Settings shows `Setup`, `Status`, `Anti-fingerprinting`, `Protections`, `Diagnostics`, `Advanced`.
  - Settings shows no `Finish setup` banner.
  - Network labels align without emoji.
  - Both pill states fit narrow screens.
  - Lockito installed + `hide_mock` ON yields green.
  - `hide_mock` OFF yields warning.
  - HideMyMock installed or absent changes nothing.
  - Widevine OFF survives setup rerun.
  - Fresh-install setup applies Widevine by default.

- Commands:

```text
.venv/Scripts/python.exe -m pytest -q
```

```text
cd xposed-module && bash run-jvm-tests.sh
```

- Type: verification only.

---
## Item 7 — persistent Reboot-required (user follow-up, NOT in codex plan above)
ITEM 7 (user follow-up) — persistent "Reboot required" state:
- The current promptReboot dialog just says "reboot" and DISAPPEARS on dismiss even if the user never rebooted.
- FIX: a PERSISTENT "Reboot required" indicator (banner/row) that stays visible until the device is ACTUALLY
  rebooted, then auto-clears when no longer needed. Minimize reboots (don't demand one when not truly needed).
- IMPLEMENTATION (leverage the v0.19.2 boot-wall attestation): when setup/scope/native install a change that
  needs a reboot, persist prefs reboot_pending_since = bootWall-stamped marker. Show the banner while
  reboot_pending. Auto-clear when the CURRENT boot started AFTER the marker (i.e. a reboot happened) OR when a
  fresh this-boot heartbeat proves the change is now live. Terse copy: "Reboot required" + one line.
